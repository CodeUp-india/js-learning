const myObj = {
    js: "Javascript",
    cpp: "c++",
    swift: "Swift"
}

for (const key in myObj) {
    //console.log(`${key} is shortcut for ${myObj[key]}`);   
}
const language = ['js', 'c++', 'C#', 'ru']
for (const key in language) {
   console.log(language[key]);
   
}